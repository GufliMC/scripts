
generateBukkitPluginDescription {
    doLast {
        copy {
            from 'build/generated/plugin-yml/Bukkit/'
            into 'src/main/resources'
        }
    }
}

processResources.dependsOn.add(generateBukkitPluginDescription)